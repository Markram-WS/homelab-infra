apiVersion: v1
kind: Service
metadata:
  name: postgresql
  namespace: database
  annotations:
    tailscale.com/expose: "true"
    tailscale.com/hostname: postgresql
spec:
  ports:
    - port: 5432
      targetPort: 5432
      name: postgres
  selector:
    app: postgresql
